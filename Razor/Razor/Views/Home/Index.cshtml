@model List<Razor.Models.Course>

<!DOCTYPE html>

<html>
<head>
    <meta home="viewport" content="width=device-width"/>
    <title>Index</title>
</head>
<body>
    <div>
        @*Model.Message *@

        <ul>
            @foreach (var item in Model)
            {
                <li>
                    @Html.Partial("_PartialWithModel", item)
                </li>
            }
        </ul>

        @if (Model.Count() == 0)
        {
            <span>
                Brak kursów!
            </span>
        }
        else
        {
            var i = 12;
            <span>
                @i
            </span>
        }

        <span @if (Model.Count==0){<text> style = "display: none" </text> }>Chwilowy brak kursów</span>
        
        @Html.Action("GetTimePartial")
        @Html.Action("GetTime")
    </div>
</body>
</html>

